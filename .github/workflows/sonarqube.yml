name: SonarQube SAST Analysis

on:
  push:
    branches: [ main, test, dev ]
  pull_request:
    branches: [ main, test, dev ]
  workflow_dispatch:

jobs:
  sonarqube:
    name: 🔍 Análisis SAST
    runs-on: ubuntu-latest
    timeout-minutes: 45
    
    services:
      sonarqube:
        image: sonarqube:10-community
        ports:
          - 9000:9000
        env:
          SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
          SONAR_SEARCH_JAVAADDITIONALOPTS: "-Xms512m -Xmx512m"
        options: >-
          --health-cmd "curl -sf http://localhost:9000/api/system/status || exit 1"
          --health-interval 15s
          --health-timeout 10s
          --health-retries 20

    steps:
      # ==========================================
      # 1. CHECKOUT
      # ==========================================
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ==========================================
      # 2. CONFIGURAR NODE.JS (SIN CACHE)
      # ==========================================
      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # ==========================================
      # 3. INSTALAR DEPENDENCIAS (NPM INSTALL)
      # ==========================================
      - name: 📦 Instalar dependencias
        run: |
          echo "📦 Instalando dependencias..."
          # Se usa npm install en lugar de npm ci porque no hay package-lock.json
          npm install --ignore-scripts
          echo "✅ Dependencias instaladas"

      # ==========================================
      # 4. ESPERAR SONARQUBE
      # ==========================================
      - name: ⏳ Esperar SonarQube
        run: |
          echo "⏳ Esperando SonarQube..."
          for i in {1..60}; do
            STATUS=$(curl -sf http://localhost:9000/api/system/status 2>/dev/null | jq -r '.status' 2>/dev/null || echo "waiting")
            if [ "$STATUS" = "UP" ]; then
              echo "✅ SonarQube listo"
              break
            fi
            echo "  Esperando... ($i/60)"
            sleep 5
          done
          
          if ! curl -sf http://localhost:9000/api/system/status | grep -q "UP"; then
            echo "❌ SonarQube no inició"
            exit 1
          fi

      # ==========================================
      # 5. CONFIGURAR SONARQUBE (Token + QG)
      # ==========================================
      - name: 🔧 Configurar SonarQube
        run: |
          # Cambiar contraseña
          curl -sf -X POST "http://localhost:9000/api/users/change_password" \
            -u "admin:admin" \
            -d "login=admin&previousPassword=admin&password=SastPass123!" || true
          
          sleep 2
          
          # Generar token
          TOKEN=$(curl -sf -X POST "http://localhost:9000/api/user_tokens/generate" \
            -u "admin:SastPass123!" \
            -d "name=ci-$(date +%s)&type=GLOBAL_ANALYSIS_TOKEN" | jq -r '.token')
          
          echo "SONAR_TOKEN=$TOKEN" >> $GITHUB_ENV
          
          if [ "$TOKEN" = "null" ] || [ -z "$TOKEN" ]; then
            echo "❌ Error generando token"
            exit 1
          fi
          echo "✅ Token generado"
          
          # Crear Quality Gate
          QG_ID=$(curl -sf -X POST "http://localhost:9000/api/qualitygates/create" \
            -u "admin:SastPass123!" \
            -d "name=SAST Security Standard" | jq -r '.id')
          
          # Condiciones del Quality Gate
          curl -sf -X POST "http://localhost:9000/api/qualitygates/create_condition" -u "admin:SastPass123!" -d "gateId=$QG_ID&metric=new_bugs&op=LT&error=1"
          curl -sf -X POST "http://localhost:9000/api/qualitygates/create_condition" -u "admin:SastPass123!" -d "gateId=$QG_ID&metric=new_vulnerabilities&op=LT&error=1"
          curl -sf -X POST "http://localhost:9000/api/qualitygates/create_condition" -u "admin:SastPass123!" -d "gateId=$QG_ID&metric=new_security_hotspots_reviewed&op=LT&error=1"
          curl -sf -X POST "http://localhost:9000/api/qualitygates/create_condition" -u "admin:SastPass123!" -d "gateId=$QG_ID&metric=new_code_smells&op=LT&warning=10&error=20"
          curl -sf -X POST "http://localhost:9000/api/qualitygates/create_condition" -u "admin:SastPass123!" -d "gateId=$QG_ID&metric=duplicated_lines_density&op=GT&warning=5&error=10"
          curl -sf -X POST "http://localhost:9000/api/qualitygates/create_condition" -u "admin:SastPass123!" -d "gateId=$QG_ID&metric=sqale_rating&op=LT&error=2"
          curl -sf -X POST "http://localhost:9000/api/qualitygates/create_condition" -u "admin:SastPass123!" -d "gateId=$QG_ID&metric=reliability_rating&op=LT&error=2"
          curl -sf -X POST "http://localhost:9000/api/qualitygates/create_condition" -u "admin:SastPass123!" -d "gateId=$QG_ID&metric=security_rating&op=LT&error=2"
          
          # Establecer como defecto
          curl -sf -X POST "http://localhost:9000/api/qualitygates/set_as_default" -u "admin:SastPass123!" -d "id=$QG_ID"
          
          echo "✅ Quality Gate configurado"

      # ==========================================
      # 6. TESTS Y COVERAGE POR MÓDULO
      # ==========================================
      - name: 🧪 Tests - Auth Service
        working-directory: apps/auth-service
        run: |
          mkdir -p ../../coverage/auth-service
          if [ -f "jest.config.js" ] || [ -f "jest.config.ts" ]; then
            npx jest --coverage --coverageDirectory=../../coverage/auth-service --coverageReporters=lcov --ci --forceExit 2>/dev/null || true
          fi
          [ ! -f "../../coverage/auth-service/lcov.info" ] && echo "" > ../../coverage/auth-service/lcov.info
          echo "✅ Auth Service completado"

      - name: 🧪 Tests - Frontend
        working-directory: apps/frontend
        run: |
          mkdir -p ../../coverage/frontend
          if [ -f "jest.config.js" ] || [ -f "jest.config.ts" ]; then
            npx jest --coverage --coverageDirectory=../../coverage/frontend --coverageReporters=lcov --ci --forceExit 2>/dev/null || true
          fi
          [ ! -f "../../coverage/frontend/lcov.info" ] && echo "" > ../../coverage/frontend/lcov.info
          echo "✅ Frontend completado"

      - name: 🧪 Tests - Products
        working-directory: apps/products
        run: |
          mkdir -p ../../coverage/products
          if [ -f "jest.config.js" ] || [ -f "jest.config.ts" ]; then
            npx jest --coverage --coverageDirectory=../../coverage/products --coverageReporters=lcov --ci --forceExit 2>/dev/null || true
          fi
          [ ! -f "../../coverage/products/lcov.info" ] && echo "" > ../../coverage/products/lcov.info
          echo "✅ Products completado"

      - name: 🧪 Tests - Análisis Vulnerabilidades
        working-directory: apps/analisis-vulnerabilidades
        run: |
          mkdir -p ../../coverage/analisis-vulnerabilidades
          if [ -f "jest.config.js" ] || [ -f "jest.config.ts" ]; then
            npx jest --coverage --coverageDirectory=../../coverage/analisis-vulnerabilidades --coverageReporters=lcov --ci --forceExit 2>/dev/null || true
          fi
          [ ! -f "../../coverage/analisis-vulnerabilidades/lcov.info" ] && echo "" > ../../coverage/analisis-vulnerabilidades/lcov.info
          echo "✅ Análisis Vulnerabilidades completado"

      # ==========================================
      # 7. ANÁLISIS SONARQUBE
      # ==========================================
      - name: 🔍 Ejecutar SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v2
        env:
          SONAR_TOKEN: ${{ env.SONAR_TOKEN }}
          SONAR_HOST_URL: http://localhost:9000
          SONAR_SCANNER_OPTS: >
            -Dsonar.qualitygate.wait=true
            -Dsonar.qualitygate.timeout=300

      # ==========================================
      # 8. VERIFICAR QUALITY GATE
      # ==========================================
      - name: ✅ Verificar Quality Gate
        if: always()
        run: |
          # Verificar que SonarQube sigue respondiendo antes de consultar
          if ! curl -sf http://localhost:9000/api/system/status > /dev/null 2>&1; then
            echo "⚠️ SonarQube no está disponible (el workflow pudo fallar antes de este paso)"
            exit 0
          fi
          
          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║          RESULTADO QUALITY GATE - SAST                  ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          
          QG=$(curl -sf "http://localhost:9000/api/qualitygates/project_status?projectKey=taller-sast" \
            -u "admin:SastPass123!" 2>/dev/null)
          
          if [ -z "$QG" ]; then
            echo "🔄 Primer análisis completado"
            echo "Los Quality Gates se evaluarán en el próximo análisis"
            exit 0
          fi
          
          STATUS=$(echo "$QG" | jq -r '.projectStatus.status // "UNKNOWN"')
          echo "Rama: ${{ github.ref_name }}"
          echo "Commit: ${GITHUB_SHA:0:7}"
          echo "Estado: $STATUS"
          
          COND_COUNT=$(echo "$QG" | jq '.projectStatus.conditions | length' 2>/dev/null || echo "0")
          if [ "$COND_COUNT" != "0" ] && [ "$COND_COUNT" != "null" ]; then
            echo ""
            echo "Condiciones:"
            echo "$QG" | jq -r '.projectStatus.conditions[]? | "  • \(.metric): \(.status) [\(.actualValue)]"' 2>/dev/null
          fi
          
          if [ "$STATUS" = "ERROR" ]; then
            echo ""
            echo "❌ QUALITY GATE FALLADO"
            exit 1
          fi
          
          echo ""
          echo "✅ Análisis completado - Status: $STATUS"

      # ==========================================
      # 9. RESUMEN GITHUB
      # ==========================================
      - name: 📊 Resumen en GitHub
        if: always()
        run: |
          # Verificar que SonarQube sigue respondiendo
          if ! curl -sf http://localhost:9000/api/system/status > /dev/null 2>&1; then
            echo "# ⚠️ Análisis SAST Incompleto" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "El workflow falló antes de que SonarQube pudiera completar el análisis." >> $GITHUB_STEP_SUMMARY
            echo "Revisa los pasos anteriores para más detalles." >> $GITHUB_STEP_SUMMARY
            exit 0
          fi
          
          QG_RAW=$(curl -sf "http://localhost:9000/api/qualitygates/project_status?projectKey=taller-sast" \
            -u "admin:SastPass123!" 2>/dev/null)
          
          if [ -z "$QG_RAW" ]; then
            STATUS="PENDING"
            CONDITIONS_TABLE="*Primer análisis - sin datos previos*"
          else
            STATUS=$(echo "$QG_RAW" | jq -r '.projectStatus.status // "PENDING"')
            CONDITIONS_COUNT=$(echo "$QG_RAW" | jq '.projectStatus.conditions | length' 2>/dev/null || echo "0")
            
            if [ "$CONDITIONS_COUNT" = "null" ] || [ "$CONDITIONS_COUNT" = "0" ]; then
              CONDITIONS_TABLE="*Sin condiciones evaluadas*"
            else
              CONDITIONS_TABLE=$(echo "$QG_RAW" | jq -r '.projectStatus.conditions[]? | "- **\(.metric)**: `\(.status)` (valor: `\(.actualValue)`)"' 2>/dev/null || echo "*Error leyendo condiciones*")
            fi
          fi
          
          case "$STATUS" in
            OK) EMOJI="✅" ;;
            ERROR|FAILED) EMOJI="❌" ;;
            WARNING) EMOJI="⚠️" ;;
            PENDING|*) EMOJI="🔄" ;;
          esac
          
          cat >> $GITHUB_STEP_SUMMARY << EOF
          # ${EMOJI} Análisis SAST - Taller SAST
          
          ## Módulos Analizados
          
          | Módulo | Tipo | Estado |
          |--------|------|--------|
          | 📁 auth-service | NestJS | ✅ Analizado |
          | 📁 frontend | React | ✅ Analizado |
          | 📁 products | NestJS | ✅ Analizado |
          | 📁 analisis-vulnerabilidades | Utils | ✅ Analizado |
          
          ## Quality Gate: **${STATUS}**
          
          <details>
          <summary>📋 Ver condiciones evaluadas</summary>
          
          ${CONDITIONS_TABLE}
          
          </details>
          
          ---
          *Rama: \`${{ github.ref_name }}\` | Commit: \`${GITHUB_SHA:0:7}\`*
          EOF