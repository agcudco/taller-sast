resolver 127.0.0.11 valid=10s ipv6=off;

server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # SPA – enruta todas las peticiones a index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy inverso hacia los microservicios (opcional, evita problemas de CORS en producción)
    location /api/auth/ {
        set $upstream_auth auth-service:3000;
        proxy_pass http://$upstream_auth;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api/products/ {
        set $upstream_products product-service:3000;
        proxy_pass http://$upstream_products;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Compresión
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}